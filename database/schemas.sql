CREATE DATABASE globalmentors;
\c globalmentors;

-- CREATE TABLE users (
--   id SERIAL,
--   googleId TEXT,
--   fullName TEXT,
--   gender TEXT,
--   ratings INTEGER,
--   bio TEXT,
--   isMentor BOOLEAN,
--   isMentee BOOLEAN,
--   mentors TEXT [],
--   mentees TEXT [],
--   blocked TEXT [],
--   location JSON NOT NULL
-- );

/* 
  open postgres
  select port number (not server port)
  select database name >> opens terminal
  \dt >> sees all current tables
  \q >> leave postgres
  
  open psequel
  select same port number
 */